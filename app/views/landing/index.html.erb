<%= button_to "Create a New User", "/register", method: :get %>
<%= button_to "Log In", "/users/login", method: :get %>
<p>Existing Users:</p>

<% @users.each do |user|%>
  <%= link_to "#{user.email}'s Dashboard", "/users/#{user.id}"%>
  <br>
<% end %>
